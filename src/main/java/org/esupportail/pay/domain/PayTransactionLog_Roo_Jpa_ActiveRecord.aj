// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.esupportail.pay.domain;

import java.util.List;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import org.esupportail.pay.domain.PayTransactionLog;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PayTransactionLog_Roo_Jpa_ActiveRecord {
    
    @PersistenceContext
    transient EntityManager PayTransactionLog.entityManager;
    
    public static final List<String> PayTransactionLog.fieldNames4OrderClauseFilter = java.util.Arrays.asList("payEvtMontant", "transactionDate", "field1", "field2", "mail", "reference", "montant", "auto", "erreur", "idtrans", "signature", "mailSent");
    
    public static final EntityManager PayTransactionLog.entityManager() {
        EntityManager em = new PayTransactionLog().entityManager;
        if (em == null) throw new IllegalStateException("Entity manager has not been injected (is the Spring Aspects JAR configured as an AJC/AJDT aspects library?)");
        return em;
    }
    
    public static long PayTransactionLog.countPayTransactionLogs() {
        return entityManager().createQuery("SELECT COUNT(o) FROM PayTransactionLog o", Long.class).getSingleResult();
    }
    
    public static List<PayTransactionLog> PayTransactionLog.findAllPayTransactionLogs() {
        return entityManager().createQuery("SELECT o FROM PayTransactionLog o", PayTransactionLog.class).getResultList();
    }
    
    public static List<PayTransactionLog> PayTransactionLog.findAllPayTransactionLogs(String sortFieldName, String sortOrder) {
        String jpaQuery = "SELECT o FROM PayTransactionLog o";
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            jpaQuery = jpaQuery + " ORDER BY " + sortFieldName;
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                jpaQuery = jpaQuery + " " + sortOrder;
            }
        }
        return entityManager().createQuery(jpaQuery, PayTransactionLog.class).getResultList();
    }
    
    public static PayTransactionLog PayTransactionLog.findPayTransactionLog(Long id) {
        if (id == null) return null;
        return entityManager().find(PayTransactionLog.class, id);
    }
    
    public static List<PayTransactionLog> PayTransactionLog.findPayTransactionLogEntries(int firstResult, int maxResults) {
        return entityManager().createQuery("SELECT o FROM PayTransactionLog o", PayTransactionLog.class).setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    }
    
    public static List<PayTransactionLog> PayTransactionLog.findPayTransactionLogEntries(int firstResult, int maxResults, String sortFieldName, String sortOrder) {
        String jpaQuery = "SELECT o FROM PayTransactionLog o";
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            jpaQuery = jpaQuery + " ORDER BY " + sortFieldName;
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                jpaQuery = jpaQuery + " " + sortOrder;
            }
        }
        return entityManager().createQuery(jpaQuery, PayTransactionLog.class).setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    }
    
    @Transactional
    public void PayTransactionLog.persist() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.persist(this);
    }
    
    @Transactional
    public void PayTransactionLog.remove() {
        if (this.entityManager == null) this.entityManager = entityManager();
        if (this.entityManager.contains(this)) {
            this.entityManager.remove(this);
        } else {
            PayTransactionLog attached = PayTransactionLog.findPayTransactionLog(this.id);
            this.entityManager.remove(attached);
        }
    }
    
    @Transactional
    public void PayTransactionLog.flush() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.flush();
    }
    
    @Transactional
    public void PayTransactionLog.clear() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.clear();
    }
    
    @Transactional
    public PayTransactionLog PayTransactionLog.merge() {
        if (this.entityManager == null) this.entityManager = entityManager();
        PayTransactionLog merged = this.entityManager.merge(this);
        this.entityManager.flush();
        return merged;
    }
    
}
